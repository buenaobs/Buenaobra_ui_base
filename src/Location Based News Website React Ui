export default function LocationBasedNewsWebsite() {
  const categories = [
    'Nation',
    'Sports',
    'Events',
    'Crime',
    'Education',
    'Community',
  ];

  const newsCards = [
    {
      title: 'NU Rookie Sam Cantada Gains Experience from UAAP Finals Loss',
      tag: 'Sports',
      image:
        'https://images.unsplash.com/photo-1547347298-4074fc3086f0?q=80&w=1200&auto=format&fit=crop',
    },
    {
      title: 'Navotas landfill fire extinguished — Marcos',
      tag: 'Community',
      image:
        'https://images.unsplash.com/photo-1506744038136-46273834b3fb?q=80&w=1200&auto=format&fit=crop',
    },
    {
      title: 'UK Embassy in PH urged to revoke businessman’s visa over trafficking',
      tag: 'Nation',
      image:
        'https://images.unsplash.com/photo-1520607162513-77705c0f0d4a?q=80&w=1200&auto=format&fit=crop',
    },
  ];

  return (
    <div className="min-h-screen bg-green-700 flex text-black font-sans">
      <aside className="w-72 bg-white p-6 flex flex-col gap-8 shadow-xl">
        <div>
          <h1 className="text-3xl font-bold mb-8">Barney News</h1>

          <nav className="space-y-4 text-lg font-semibold">
            <div className="bg-gray-100 rounded-2xl px-4 py-3">Home</div>
            <div className="px-4 py-3">Trending</div>
            <div className="px-4 py-3">Incident Maps</div>
            <div className="px-4 py-3">Maps</div>
            <div className="px-4 py-3">Categories</div>
            <div className="px-4 py-3">Favorites</div>
            <div className="px-4 py-3">Saved Locations</div>
            <div className="px-4 py-3">Multimedia</div>
          </nav>
        </div>

        <div>
          <h2 className="text-2xl font-bold mb-4">Categories</h2>
          <div className="space-y-4">
            {categories.map((category) => (
              <div
                key={category}
                className="flex items-center gap-3 bg-gray-100 rounded-xl px-4 py-3 font-semibold"
              >
                <div className="w-4 h-4 rounded-full bg-green-600"></div>
                {category}
              </div>
            ))}
          </div>
        </div>
      </aside>

      <main className="flex-1 p-8 overflow-y-auto">
        <header className="flex items-center justify-between bg-white rounded-3xl px-6 py-4 shadow-lg mb-8">
          <input
            type="text"
            placeholder="Search news by keyword, location, or topic..."
            className="w-1/2 bg-gray-100 rounded-full px-5 py-3 outline-none"
          />

          <div className="flex items-center gap-6 font-semibold text-lg">
            <span>📍 Lucena City</span>
            <span>🌙</span>
            <span>🔔</span>
            <div className="bg-gray-100 rounded-full px-4 py-2 flex items-center gap-2">
              <span>🦖</span>
              Barney
            </div>
          </div>
        </header>

        <section className="grid grid-cols-3 gap-6 mb-10">
          <div className="col-span-2 relative rounded-[2rem] overflow-hidden shadow-2xl h-[420px]">
            <img
              src="https://images.unsplash.com/photo-1495020689067-958852a7765e?q=80&w=1600&auto=format&fit=crop"
              alt="headline"
              className="w-full h-full object-cover"
            />

            <div className="absolute inset-0 bg-black/40 flex flex-col justify-end p-10 text-white">
              <span className="bg-blue-600 w-fit px-4 py-2 rounded-xl text-sm font-bold mb-5">
                RECENT
              </span>

              <h1 className="text-5xl font-extrabold leading-tight max-w-4xl">
                Padilla: I’ll Keep Defending Dutertes till Senate becomes impeachment court
              </h1>

              <p className="mt-5 text-xl font-medium">By: Isabelle Pechay | Nation</p>
            </div>
          </div>

          <div className="space-y-6">
            <div className="bg-white rounded-[2rem] p-6 shadow-xl">
              <h2 className="text-3xl font-bold">Lucena City</h2>
              <div className="flex items-center justify-between mt-4">
                <div>
                  <div className="text-6xl font-black">25°</div>
                  <p className="text-xl font-semibold">Partly Cloudy</p>
                  <p className="text-gray-500">May 10, 2026 | 7:30 AM</p>
                </div>

                <div className="text-7xl">⛅</div>
              </div>
            </div>

            <div className="bg-white rounded-[2rem] p-6 shadow-xl">
              <h2 className="text-3xl font-bold mb-4">Trending Now</h2>

              <div className="space-y-4 font-semibold">
                <div>1. Duterte Lawyer exits, makes last bid for client’s release</div>
                <div>2. Lawyer pushing VP impeach raps wary of Senate delay</div>
                <div>3. Padilla to Dela Rosa on ICC arrest warrant rumors</div>
              </div>
            </div>
          </div>
        </section>

        <section>
          <div className="flex items-center justify-between mb-8">
            <h2 className="text-5xl font-black text-white">Latest News</h2>

            <div className="flex gap-4 overflow-x-auto">
              {['All', 'Nation', 'Sports', 'Events', 'Crime', 'More'].map((tab) => (
                <button
                  key={tab}
                  className="bg-white rounded-full px-8 py-4 font-bold shadow-lg hover:scale-105 transition"
                >
                  {tab}
                </button>
              ))}
            </div>
          </div>

          <div className="grid grid-cols-3 gap-6">
            {newsCards.map((card) => (
              <div
                key={card.title}
                className="bg-white rounded-[2rem] overflow-hidden shadow-2xl"
              >
                <img
                  src={card.image}
                  alt={card.title}
                  className="h-64 w-full object-cover"
                />

                <div className="p-6">
                  <span className="bg-orange-500 text-white text-sm px-4 py-2 rounded-full font-bold">
                    {card.tag}
                  </span>

                  <h3 className="text-2xl font-extrabold mt-5 leading-snug">
                    {card.title}
                  </h3>

                  <div className="flex items-center justify-between mt-8 text-gray-500 font-medium">
                    <span>May 10, 2026</span>
                    <span className="text-2xl">🔖</span>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </section>

        <section className="mt-16 bg-white rounded-[2rem] p-8 shadow-2xl">
          <div className="flex items-center justify-between mb-8">
            <div>
              <h2 className="text-5xl font-black">Interactive Map</h2>
              <p className="text-gray-500 text-lg mt-2">
                Explore news and incidents in your area.
              </p>
            </div>

            <button className="bg-gray-100 rounded-full px-6 py-3 font-bold">
              Last 7 Days
            </button>
          </div>

          <div className="relative rounded-[2rem] overflow-hidden h-[500px]">
            <img
              src="https://images.unsplash.com/photo-1524661135-423995f22d0b?q=80&w=1600&auto=format&fit=crop"
              alt="map"
              className="w-full h-full object-cover"
            />

            <div className="absolute left-10 top-10 bg-white rounded-[2rem] w-[360px] overflow-hidden shadow-2xl">
              <img
                src="https://images.unsplash.com/photo-1449824913935-59a10b8d2000?q=80&w=1200&auto=format&fit=crop"
                alt="incident"
                className="h-52 w-full object-cover"
              />

              <div className="p-6">
                <span className="bg-purple-600 text-white px-4 py-2 rounded-full text-sm font-bold">
                  COMMUNITY
                </span>

                <h3 className="text-2xl font-extrabold mt-5">
                  Riverbank dwellers in Lucena alerted for flood evacuations
                </h3>

                <p className="text-gray-500 mt-4">May 10, 2026</p>
              </div>
            </div>
          </div>
        </section>
      </main>
    </div>
  );
}
